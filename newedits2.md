# Front-end تغييرات مطلوبة (07-08-2026)

## 1) شاشة القيود اليومية (Journal Entries) – فلتر رقم العقد

### المطلوب
- إضافة حقل في فلتر القيود اليومية باسم **رقم العقد**.
- عند إدخال رقم العقد يتم استدعاء API بالباراميتر التالي:
  - `contractNumber` (int)

### الاستدعاء
- مثال:
  - `GET api/V1/JournalEntries?contractNumber=123`

> ملاحظة: يوجد فلتر قديم باسم `sourceId` (GUID). بعد إضافة `contractNumber` يمكن للواجهة الاحتفاظ بـ `sourceId` إذا كانت مستخدمة سابقاً، لكن تأكد أن الـ UI لا يسبب تعارضاً بينهما (لأن القيم ستتطبق معاً على الـ query).


## 2) إغلاق الفترة المالية – التحويل إلى إغلاق سنوي + فتح السنة

### المطلوب تغيير واجهة المستخدم من (شهر/سنة) إلى (سنة فقط).
- إظهار السنوات بدءاً من **2025** حتى السنة الحالية
- لكل سنة: حالة (مغلقة/مفتوحة)
- زر:
  - **إغلاق السنة**
  - **فتح السنة**

### الـ Endpoints الجديدة

1) جلب قائمة السنوات:
- `GET api/V1/PeriodClosing`

2) إغلاق سنة:
- `POST api/V1/PeriodClosing/close`
- Body:
  ```json
  { "year": 2025 }
  ```

3) فتح سنة:
- `POST api/V1/PeriodClosing/open`
- Body:
  ```json
  { "year": 2025 }
  ```

4) حالة سنة (اختياري إذا كنت تستخدم القائمة):
- `GET api/V1/PeriodClosing/status?year=2025`

### تأثير ذلك على القيود
- عند محاولة ترحيل قيد داخل سنة مغلقة، سيرجع النظام خطأ (منع الترحيل/Unpost حسب منطق السنة).
- لذلك يفضّل تعطيل زر الترحيل/العمليات المحاسبية المرتبطة بالترحيل داخل الـ UI عندما تكون السنة **مغلقة**.


## 3) قيد تكلفة عقد الوساطة (Mediation) – فصل Local Cost و Agent Cost

### المطلوب في الواجهة
- عند عرض **تفاصيل القيد** المرتبط بعقد الوساطة بعد التوقيع:
  - لا تفترض أن تكلفة الوساطة تظهر كسطر مدين واحد.
  - ستظهر التكلفة كسطور منفصلة:
    - سطر مدين لـ **تكلفة محليّة (Local Cost)**.
    - سطر مدين لـ **تكلفة الوكيل (Agent Cost)**.
    - وسطر دائن بالمجموع (الحساب الدائن المستخدم حالياً في النظام).

### تنبيه UI
- تأكد أن منطق العرض (Tables/Grids) لا يجمع/يلخّص سطور التكلفة إلى سطر واحد.
- اعرض السطور كما هي مع `AccountCode/AccountName` القادمة من `JournalEntryLineDto`.

